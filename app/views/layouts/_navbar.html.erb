<!-- Navbar -->
<style>
    .nav-masthead .nav-link {
    color: rgba(255, 255, 255, .5);
    
    }

    .nav-masthead .nav-link:hover,
    .nav-masthead .nav-link:focus {
      border-bottom: .25rem solid white;
      /*border-bottom-color: rgba(255, 255, 255, .25);*/
    }

    .nav-masthead .nav-link + .nav-link {
    margin-left: 1rem;
    }

    .nav-masthead .active {
    color: #fff;
    border-bottom-color: #fff;
    }
</style>
 <header class="mb-auto">
    <div>
      <h3 class="float-md-start mb-0"><img src="<%= asset_path 'logo.png' %>" width="100px"></h3>
      <nav class="nav nav-masthead justify-content-center float-md-end">
        <a class="nav-link fw-bold py-1 px-0 active" aria-current="page" href="/">Home</a>
        <a class="nav-link fw-bold py-1 px-0 active" href="#">FAQ</a>
        <% if session[:user_id] == nil  %>
          <a class="nav-link fw-bold py-1 px-0 active" href="/login">Login/Register</a>
        <% else %>
          <a class="nav-link fw-bold py-1 px-0 active" href="/dashboard">Dashboard</a>
          <a href="/logout" class="nav-link fw-bold py-1 px-0 active"  style="color:red">Logout (<%= User.find(session[:user_id]).email_address %>)</a>
        <%end%>
      </nav>
    </div>
  </header>
    <!-- Navbar -->